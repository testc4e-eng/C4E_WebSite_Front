// Composant AspectRatio basé sur Radix UI
// Permet de maintenir un ratio fixe (largeur/hauteur) pour un contenu (images, vidéos, etc.)
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
